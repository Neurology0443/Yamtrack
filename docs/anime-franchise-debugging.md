# Anime franchise debugging

The current debugging guide lives in `docs/anime-franchise-debugging-runbook.md`.

This file is kept as a stable alias for older links. The runbook contains the service, snapshot, rule-placement, cache, alias, import-state, Docker, and test commands that used to live here.
